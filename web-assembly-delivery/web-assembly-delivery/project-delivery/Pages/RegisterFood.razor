@page "/register-food"

<div class="min-h-screen flex items-center justify-center bg-gray-50 px-4">
    <div class="w-full max-w-md bg-white p-6 rounded-xl shadow-md">
        <h3 class="text-2xl font-semibold text-gray-800 mb-6 text-center">Registrar Comida</h3>

        <EditForm Model="@foodItem" OnValidSubmit="HandleValidSubmit" class="space-y-6">
            <div class="border border-gray-300 rounded-lg p-4">
                <label for="Name" class="block text-sm font-medium text-gray-700 mb-1">Nome da comida</label>
                <InputText id="Name" @bind-Value="foodItem.Name" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Ex: Lasanha" />
            </div>

            <div class="border border-gray-300 rounded-lg p-4">
                <label for="Description" class="block text-sm font-medium text-gray-700 mb-1">Descrição</label>
                <InputText id="Description" @bind-Value="foodItem.Description" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Ex: Lasanha com molho branco" />
            </div>

            <div class="border border-gray-300 rounded-lg p-4">
                <label for="ImageUrl" class="block text-sm font-medium text-gray-700 mb-1">URL da Imagem</label>
                <InputText id="ImageUrl" @bind-Value="foodItem.ImageUrl" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="https://exemplo.com/imagem.jpg" />
            </div>

            <div class="border border-gray-300 rounded-lg p-4">
                <label for="Price" class="block text-sm font-medium text-gray-700 mb-1">Preço</label>
                <InputNumber id="Price" @bind-Value="foodItem.Price" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="R$ 00,00" />
            </div>

            <button type="submit"
                    class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-lg shadow transition duration-200">
                💾 Salvar
            </button>
        </EditForm>
    </div>
</div>

@code {
    private FoodItem foodItem = new FoodItem();

    private void HandleValidSubmit()
    {
        
    }

}
